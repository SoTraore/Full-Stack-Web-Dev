<!DOCTYPE html>
<html>

<head>
  <title>Taco Recipes</title>
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
  <h1>Taco Town 🌮</h1>

  <form action="/recipe" method="POST" class="buttons">
    <button type="submit" value="chicken" name="choice">🍗</button>
    <button type="submit" value="beef" name="choice">🥩</button>
    <button type="submit" value="fish" name="choice">🐟</button>
  </form>
  <div id="recipeContainer" class="hidden">

    <% if (!locals.data) { %>
    <!-- If no recipe yet, show: -->
    <h2>Pick your favourite taco ingredient 👆</h2>
    <% } else { %>
    <!-- Else, show the recipe name e.g. <h2>Recipe name</h2>-->
    <h2>Recipe name : <%= data.name %></h2>
    <h3>Ingredients:</h3>
    <ul id="ingredientsList">
      <li>Protein name : <%= data.ingredients.protein.name %>, protein preparation : <%= data.ingredients.protein.preparation %></li>
      <li>Salsa name : <%= data.ingredients.salsa.name %>, spiciness: <%= data.ingredients.salsa.spiciness %></li>
      <li>Toppings : </li>
      <% for (let i=0; i<data.ingredients.toppings.length; i++) { %>
        <li>name (<%= data.ingredients.toppings[i].name %>), quantite (<%= data.ingredients.toppings[i].quantity %>), ingredients (<%= data.ingredients.toppings[i].ingredients %>)</li>
      <% } %>
    </ul>

    <% } %>

  </div>
</body>

</html>